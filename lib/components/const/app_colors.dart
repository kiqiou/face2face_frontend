import 'package:flutter/material.dart';

class MyColors {
  // Основной фон — нежный нюд
  static const Color background = Color(0xFFFDF6F0); // почти белый с теплотой

  // Акцент — мягкое золото
  static const Color accent = Color(0xFFEACD76); // светлое золото

  // Цвет заголовков — светлый карамельный
  static const Color heading = Color(0xFFBFAE9F); // тёплый, но не тёмный

  // Цвет подзаголовков — бежевый
  static const Color subtitle = Color(0xFFD8CFC4); // мягкий беж

  // Цвет карточек и блоков — пудровый
  static const Color card = Color(0xFFF6E7D7); // пудрово-нюдовый

  // Цвет основного текста — серо-бежевый
  static const Color textPrimary = Color(0xFF9C8F85); // светлый, читаемый

  // Цвет вторичного текста — светло-серый
  static const Color textSecondary = Color(0xFFBFB8B2);

  // Цвет ошибок — мягкий коралл
  static const Color error = Color(0xFFF28B82); // не агрессивный

  // Цвет границ — кремовый
  static const Color border = Color(0xFFF0E6DC);

  // Цвет теней — лёгкий прозрачный
  static const Color shadow = Color(0x1A000000); // 10% чёрного

  // Цвет успеха — пастельный зелёный
  static const Color success = Color(0xFFCDEAC0);

  // Цвет для disabled-состояний — светло-бежевый
  static const Color disabled = Color(0xFFF3F0ED);
}
